<h1>
    {{model.name}}
</h1>
<p>We need a few details to identify you. Your email address will be sent to the employer with the test answers.</p>
<form class="form-horizontal">
    <div class="form-group">
        <label class="control-label col-sm-3" for="firstName">First name</label>
        <div class="col-sm-9">
            {{input name="firstName" class="form-control" value=firstName placeholder="First name"}}
        </div>
    </div>
    <div class="form-group">
        <label class="control-label col-sm-3" for="lastName">Last name</label>
        <div class="col-sm-9">
            {{input name="lastName" class="form-control" value=lastName placeholder="Last name"}}
        </div>
    </div>
    <div class="form-group">
        <label class="control-label col-sm-3" for="email">Email address</label>
        <div class="col-sm-9">
            {{input type="email" class="form-control" name="email" value=email placeholder="Email"}}
        </div>
    </div>
    <div class="form-group">
        <div class="col-sm-3"></div>
        <div class="col-sm-9">
            <label class="control-label" for="iAgree">
                {{input type="checkbox" checked=iAgree}}
                I am completing this on my own
            </label>
        </div>
    </div>
    <div class="form-group">
        <div class="col-sm-3"></div>
        <div class="col-sm-3">
            {{#link-to 'test' this class="btn btn-block btn-default"}}Cancel{{/link-to}}
            {{!-- <button class="btn btn-block btn-default">Cancel</button> --}}

        </div>
        <div class="col-sm-3">
            <button class="btn btn-block btn-primary">Start now!</button>
        </div>
        <div class="col-sm-3"></div>
    </div>

</form>